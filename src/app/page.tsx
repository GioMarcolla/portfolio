import { Box, Flex, Heading } from "@chakra-ui/react";
import { FC } from "react";

const Home: FC = () => {
    return (
        <Box>
            <Flex>
                <Heading>Home</Heading>
            </Flex>
        </Box>
    );
};

export default Home;
